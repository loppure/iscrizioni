Ciao!

Copia-incolla questo link nel browser per procedere all'iscrizione all'associazione L'oppure

<a href="{{ path("verify-email", {'code': user.code}) }}">{{ path("verify-email", {'code': user.code}) }}</a>
